name: AI Index Distribution Health

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm
      - run: npm install --legacy-peer-deps --no-audit --no-fund
      - run: npm run identity:health
      - run: npm run typecheck
      - run: npm run build
      - name: Start app
        run: |
          npm run start -- -H 127.0.0.1 -p 3000 > /tmp/ai-index-distribution.log 2>&1 &
          echo $! > /tmp/ai-index-distribution.pid
      - name: Wait for app
        run: |
          for i in $(seq 1 30); do
            if curl -fsS http://127.0.0.1:3000/en/press >/dev/null; then exit 0; fi
            sleep 2
          done
          cat /tmp/ai-index-distribution.log
          exit 1
      - run: npm run distribution:index
